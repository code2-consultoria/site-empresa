server {
    listen 8080;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Health check endpoint para incode2
    location /health {
        access_log off;
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}